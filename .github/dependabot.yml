version: 2
updates:
  # Keep the GitHub Actions used by the CI workflow up to date. Grouped into a
  # single PR so version bumps arrive as one low-noise change rather than one PR
  # per action.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - "*"
