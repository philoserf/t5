# Master Mod tables

`mastermod` contains named random-reference lookups from Book 1 pp.264–269.
`Table.Lookup` accepts the already-rolled total and optional deterministic
`<Token>` substitutions; it never rolls dice itself. Tables retain the source's
roll notation and exact contiguous bounds. Blank source cells are not invented.
