# sectorgen

Part of the interstellar mapping tier. See `internal/survey/CLAUDE.md` — it documents
`sectorgen`, `survey`, and `route` together, since `survey.Sector` is the single entry point that
composes all three.
