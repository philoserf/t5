# Go
/t5

# go-task up-to-date cache
/.task/

# macOS
.DS_Store

# Source rulebooks and their extracted text — copyrighted, not distributed (see README)
/docs/pdf/*.pdf
/docs/reference/

# Local planning/status doc — a working backlog kept per-clone, not versioned
/docs/automation-catalog.md

# stray CLI build artifacts (go build ./cmd/foo drops the binary in cwd)
/chargen
/worldgen
/systemgen
